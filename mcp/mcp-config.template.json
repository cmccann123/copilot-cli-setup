{
  "_comment": "Copy this file to ~/.copilot/mcp-config.json and replace all ${PLACEHOLDER} values. Run setup.ps1 to do this automatically with Azure Key Vault.",
  "mcpServers": {
    "playwright": {
      "_comment": "Browser automation — useful for UI testing and scraping demos. Run: npm install -g @playwright/mcp",
      "type": "stdio",
      "command": "npx",
      "args": ["@playwright/mcp@latest"],
      "env": {},
      "tools": ["*"]
    },
    "azure-mcp": {
      "_comment": "Azure resource management tools. Run: npm install -g @azure/mcp",
      "type": "stdio",
      "command": "npx",
      "args": ["@azure/mcp@latest"],
      "env": {
        "AZURE_TENANT_ID": "${AZURE_TENANT_ID}",
        "AZURE_CLIENT_ID": "${AZURE_CLIENT_ID}",
        "AZURE_CLIENT_SECRET": "${AZURE_CLIENT_SECRET}"
      },
      "tools": ["*"]
    },
    "context7": {
      "_comment": "Up-to-date library documentation lookup. No API key needed for basic use.",
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "tools": ["*"]
    },
    "microsoft-learn": {
      "_comment": "Official Microsoft Learn documentation — eliminates hallucinations on Azure/M365 topics. No API key needed. Remote HTTP endpoint.",
      "type": "http",
      "url": "https://learn.microsoft.com/api/mcp",
      "tools": ["*"]
    },
    "excalidraw": {
      "_comment": "Excalidraw diagram generation — clean, professional architecture diagrams. Run: npm install -g @scofieldfree/excalidraw-mcp",
      "type": "stdio",
      "command": "npx",
      "args": ["@scofieldfree/excalidraw-mcp@latest"],
      "env": {},
      "tools": ["*"]
    },
    "terraform": {
      "_comment": "HashiCorp official Terraform MCP — registry search, provider/module docs, workspace management, plan/apply. Requires Docker. Token only needed for HCP Terraform/Enterprise workspaces.",
      "type": "stdio",
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "-e", "TFE_TOKEN=${TFE_TOKEN}",
        "-e", "TFE_ADDRESS=${TFE_ADDRESS}",
        "hashicorp/terraform-mcp-server:latest"
      ],
      "env": {},
      "tools": ["*"]
    },
    "azure-devops": {
      "_comment": "Azure DevOps MCP — repos, work items, pipelines, boards. Run: npm install -g @tiberriver256/mcp-server-azure-devops. Set AZURE_DEVOPS_ORG_URL (e.g. https://dev.azure.com/your-org) and AZURE_DEVOPS_PAT.",
      "type": "stdio",
      "command": "npx",
      "args": ["@tiberriver256/mcp-server-azure-devops@latest"],
      "env": {
        "AZURE_DEVOPS_ORG_URL": "${AZURE_DEVOPS_ORG_URL}",
        "AZURE_DEVOPS_PAT": "${AZURE_DEVOPS_PAT}"
      },
      "tools": ["*"]
    },
    "vault": {
      "_comment": "HashiCorp official Vault MCP — KV secrets, mount management. Requires Docker and a running Vault instance. Set VAULT_ADDR and VAULT_TOKEN.",
      "type": "stdio",
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "-e", "VAULT_ADDR=${VAULT_ADDR}",
        "-e", "VAULT_TOKEN=${VAULT_TOKEN}",
        "hashicorp/vault-mcp-server:latest"
      ],
      "env": {},
      "tools": ["*"]
    }
  }
}
